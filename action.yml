name: Hello World
description: Greet a person

inputs:
  who-to-greet:
    description: Person to greet
    required: false
    default: World

runs:
  using: composite
  steps:
    - name: Print greeting
      shell: bash
      run: echo "Hello, ${{ inputs.who-to-greet }}!"
